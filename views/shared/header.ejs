<nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">

    <button class="navbar-toggler collapsed" type="button" data-toggle="collapse" data-target="#navbarsExampleDefault"
            aria-controls="navbarsExampleDefault" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="navbar-collapse collapse" id="navbarsExampleDefault" style="">
        <ul class="navbar-nav mr-auto">

            <li class="nav-item active">
                <a class="nav-link" href="/">Home</a>
            </li>

            <% if (userLogin) { %>
                <li class="nav-item active">
                    <a class="nav-link" href="/dashboard">Dashboard</a>
                </li>
            <% } %>

        </ul>

        <% if (userLogin) { %>
            <a href="/profile" class="btn btn-success my-2 my-sm-0 mr-2">Profile</a>
        <% } %>

        <a href="/<%= userLogin ? 'logout' : 'login' %>" class="btn btn-outline-success my-2 my-sm-0 mr-2"><%=
            userLogin ?
                    'Logout' : 'login' %></a>

        <% if (!userLogin) { %>
            <a href="/register" class="btn btn-success my-2 my-sm-0">Register</a>
        <% } %>

    </div>
</nav>
